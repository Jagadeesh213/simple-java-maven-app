node {

stage (‘Prepare environment’) {

git branch: ‘development’, url: ‘https://github.com/Jagadeesh213/simple-java-maven-app.git’

sh ‘npm install’

}

stage (‘Code analyse’) {

sh ‘echo “Run some lints”’

}

stage (‘Unit test’) {

sh ‘echo “Tests will back”’

}

stage (‘Build’) {

sh ‘npm run clean’

sh ‘npm run build’

}

}
stage (‘Deploy’) {

sh ‘ssh ubuntu@3.109.32.144 rm -rf /var/www/temp_deploy/dist/’

sh ‘ssh ubuntu@3.109.32.144 mkdir -p /var/www/temp_deploy’

sh ‘scp -r dist ubuntu@3.109.32.144:/var/www/temp_deploy/dist/’

sh ‘ssh ubuntu@3.109.32.144 “rm -rf /var/www/example.com/dist/ && mv /var/www/temp_deploy/dist/ /var/www/example.com/”’

}

}

